"use client";

import { GroupList } from "@linkall/ui";

/** State organizing hubs — the successor to the legacy Ω-prefixed groups. */
export default function StatesPage() {
  return <GroupList kind="state" title="State Hubs" />;
}
